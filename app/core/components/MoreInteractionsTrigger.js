import React, { Component, PropTypes } from 'react';

const MoreInteractionsTrigger = (props) => {
  return(
    <section className='component__more__interactions'>
    </section>
  );
}

MoreInteractionsTrigger.PropTypes = {
  user: PropTypes.object
}

export default MoreInteractionsTrigger;
