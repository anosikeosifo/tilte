import React, { Component, PropTypes } from 'react';

const CommentTrigger = (props) => {
  return(
    <section className='component__comment__triger'>
    </section>
  );
}

CommentTrigger.PropTypes = {
  user: PropTypes.object
}

export default CommentTrigger;
